name: Deploy jokedle-web to VPS

on:
  pull_request:
    types: [closed]
    branches: ["main"]
  # lets you run the first deploy (and any manual redeploy) without a PR
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: jokedle-web-deploy
  cancel-in-progress: true

jobs:
  deploy:
    # only run when the PR was merged (not just closed), or on manual dispatch
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.pull_request.merged == true }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GH_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./jokedle-web
          push: true
          tags: |
            ghcr.io/cade-gray/jokedle-web:latest
            ghcr.io/cade-gray/jokedle-web:${{ github.sha }}

      - name: Deploy on VPS (docker compose pull + up)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            set -e
            cd /srv/jokedle-web
            docker compose pull
            docker compose up -d
            docker image prune -f
